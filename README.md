# homebrew-siftr

Homebrew Tap for Siftr, a photo culling and similarity analysis tool.

## Installation

To add this tap to your Homebrew installation:

```bash
brew tap SuhoPark-Icloud/siftr
```

Then, install Siftr Cask:

```bash
brew install --cask siftr
```
